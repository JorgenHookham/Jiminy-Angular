define([

    'jquery',
    'underscore',
    'backbone'

], function ($, _, Backbone) {

    /*
     * This is what Jiminy knows about each of your virtues.
     */

    var VirtueModel = Backbone.Model.extend({});

    return VirtueModel;

});